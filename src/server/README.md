# 服务器验证

后台浏览器验收启动正式系统插件 API，只允许本机独立测试库。
社区运行时目录在此验证中为空，不启动监督器，不改动公网租户。
# 静态资源传输

内容指纹资源使用一年 immutable 缓存，入口 HTML 始终重新验证；此策略只作用于静态文件，不覆盖鉴权 API 或插件票据资源。`static_files.rs` 优先发送构建时生成的 gzip 文件。发布新构建前执行 `node deploy/prepare-web.cjs`，使用 HTML 解析器添加 Wasm 预加载并预压缩静态资源，再合并历史版本资源。
